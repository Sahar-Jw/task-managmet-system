// Controls which value column a settings row actually uses: the bilingual
// string pair (valueAr/valueEn) or the single numeric column (valueNumber).
export enum SettingValueType {
  STRING = 'string',
  NUMBER = 'number',
}
