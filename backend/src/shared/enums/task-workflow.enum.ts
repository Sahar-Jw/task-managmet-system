export enum TaskWorkflowMode {
  ALL_AVAILABLE = 'all_available',
  GUIDED = 'guided',
}

export enum TaskWorkflowActionKey {
  START = 'start',
  SUBMIT_APPROVAL = 'submit_approval',
  COMPLETE = 'complete',
  FINISH = 'finish',
  ARCHIVE = 'archive',
}